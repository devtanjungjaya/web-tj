---
label: Kontak admin
hide_body: true
fields:
- name: admins
  type: list
  config:
    use_select: false
    min: 
    max: 
  description: 'Nama admin (misal: "Admin Cikadu")'
  label: Admin
- name: contacts
  type: field_group_list
  fields:
  - name: admin
    type: select
    default: []
    config:
      required: false
      options: []
      source:
        type: documents
        section: kontak-admin
        file: data/contact-admin.yml
        path: admins
    label: Admin
  - name: contact
    type: field_group
    config: {}
    fields:
    - name: phoneNumbers
      type: list
      config:
        use_select: false
        min: 
        max: 
      label: List nomor telepon
    - name: whatsapp
      type: boolean
      label: Ada whatsapp
      description: Centang kalau nomor ini bisa dihubungi lewat Whatsapp
      default: false
    label: Kontak
  config:
    min: 
    max: 
    labelField: admin
  label: Kontak admin
pages:
- data/contact-admin.yml
