---
label: Kontak Kampung
hide_body: true
fields:
- name: contacts
  type: field_group_list
  fields:
  - name: names
    type: text
    config:
      required: false
    label: Nama kampung
    description: 'Masukkan nama kampung (contoh: cipanon)'
  - name: email
    type: text
    config:
      required: false
    label: Email
    description: Email kampung
  - name: telephone_numbers
    type: text
    config:
      required: false
    label: Nomor telepon
    description: Nomor telepon kampung
  - name: address
    type: text
    config:
      required: false
    label: Alamat
    description: Alamat kampung
  config:
    min: 
    max: 
    labelField: names
  label: Kontak kampung
pages:
- data/contact.yml
